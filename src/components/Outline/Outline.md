# Outline.tsx

## 作用

右侧大纲面板，展示当前文档的标题结构，支持按层级折叠。

## 为什么这么实现

- 无标题文字，只有右对齐的折叠按钮
- 平铺列表展示所有标题，右侧 badge 显示层级数字
- 折叠逻辑按层级维度操作（点击某层级 badge 折叠该层级以下的所有标题）
- 折叠面板按钮设置 `isOutlineCollapsedAtom`，由 Layout 控制面板显隐
