# EditorTabs.tsx

## 作用

编辑器顶部文件标签栏，展示已打开文件，支持切换和关闭。

## 为什么这么实现

- 只有一个文件打开时隐藏标签栏，减少视觉干扰
- 无边框设计，仅通过字重和颜色区分活动/非活动标签
- 脏文件用圆点标记（●），关闭按钮默认半透明 hover 时显现

## 视图状态保持
- 切换标签时自动保存并恢复编辑器滚动位置和光标选区
- 使用全局 `editorViewStateCache`（Map）按文件路径缓存 `scrollTop`、`scrollLeft`、`selection`
- 文件关闭时自动清除对应缓存，避免内存泄漏
