# PanelResizer.tsx

## 作用

侧边栏与编辑器之间的可拖拽分隔条，用于调整侧边栏宽度。

## 为什么这么实现

- 默认透明，hover/拖拽时才显示 1px 细线，保持极简视觉风格
- 拖拽区域 2px，视觉线条 1px，不占用多余空间
- 拖拽过程中通过 document 级事件监听实现流畅体验（避免鼠标移出元素后丢失事件）
- 组件本身只负责拖拽交互，宽度计算和折叠阈值逻辑由 Layout 层处理
