# DefaultAppToast.tsx

## 作用

右下角提示组件，引导用户将 marc-down 设为 .md 文件的默认打开方式。

## 为什么这么实现

- absolute 定位在右下角，不占据布局流，不影响三栏结构
- 一行紧凑设计（padding 6px 10px, fontSize 11px），不打扰编辑体验
- 使用 localStorage 记住"不再显示"状态，避免每次启动重复提示
- 纯前端判断（无后端检测是否已设为默认），因为跨平台检测默认应用过于复杂且不可靠
- 提供 X 按钮一键关闭，操作明确
