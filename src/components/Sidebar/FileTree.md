# FileTree.tsx

## 作用

将文件树数据结构展平为一维列表渲染，管理文件夹的展开/收起状态。

## 为什么这么实现

- 平铺列表而非嵌套递归组件，性能更好且便于控制布局
- 文件夹默认折叠，用 `expandedPaths` 记录已展开的路径；用户主动点击才展开
- 使用 `useMemo` 缓存展平结果，仅在树数据或展开状态变化时重算
- 展开状态用 `Set<string>` 存储路径，O(1) 查找效率
