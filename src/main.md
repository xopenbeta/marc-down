# main.tsx

## 作用

应用入口文件，挂载 React 根组件并初始化全局状态管理。

## 为什么这么实现

- 使用 Jotai 的 `Provider` 包裹整个应用，提供原子化状态管理的上下文
- 不使用 `StrictMode`，老鸟不用这个
- 全局样式在此处导入，确保所有组件共享统一的 CSS 变量
- KaTeX CSS 在此处导入，为编辑器中的数学公式渲染提供字体和排版样式
